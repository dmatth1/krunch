# Krunch Python deps. Torch is preinstalled in the
# pytorch/pytorch:2.4.1-cuda12.4-cudnn9-devel base image — pip auto-skips it.

rwkv==0.8.26
tokenizers==0.19.1
constriction==0.4.2

# Job mode + S3 I/O for `krunch submit`
boto3==1.34.0

# Utilities
numpy==1.26.4
